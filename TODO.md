- ✅ 09-05 04:05 **claude-kit 插件 vagaa-dgx v0.1.0 建成并推送**(7 commit,`claude plugin validate --strict` 全过,shellcheck 过,离线行为测试过,密钥/地址扫描干净):plugin.json(userConfig 四项,两条 http MCP `media`/`web-evidence`)、skills media-production(脱敏)/web-evidence、hooks SessionStart 预检(`CLAUDE_PLUGIN_OPTION_*`,探 /healthz 或 /health,失败只输出一句)+ PostToolUse 回灌 `作品已入库:<url>`、commands media-status/web-budget、marketplace.json(source 用相对路径字符串;2.1.259 不接受 `{"type":"local"}`)。**规范实测**:MCP 工具名保留连字符 `mcp__plugin_vagaa-dgx_media__…`;`plugin details` 对 inline mcpServers 显示 0 是显示问题(`mcp list` 能注册);hook exec 形 = `command` + `args`;两个 commands 会作为可被模型调用的 skill(如需仅用户可用加 `disable-model-invocation: true`)。**未装到用户级**,本机 CC 对照测试待做。
